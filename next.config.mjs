/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'tailus.io',
            'avatars.githubusercontent.com',
        ]
    },
};


export default nextConfig;
